//-- steps for accordion
// 1. fetch the data using map
// 2. use state for selected --- const [selected, setSelected] = useState(null); and apply onclick on question passing id
//3. if you click on question then for selected dataitem.id answer will be displayed
//  {selected === dataItem.id ? (
//   <div className="accordion-content">{dataItem.answer}</div>) : null}
